<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @school_class.name %>
</p>

<h2>Newsy</h2>
<% @school_class.news.each do |one| %>
<p>News #<%= one.id %>: <%= one.title %></p>
<%end
%>

<%= link_to 'Edit', edit_school_class_path(@school_class) %> |
<%= link_to 'Back', school_classes_path %>
