<%= form_for(@task_document) do |f| %>
  <% if @task_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_document.errors.count, "error") %> prohibited this task_document from being saved:</h2>

      <ul>
      <% @task_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :is_published %><br />
    <%= f.check_box :is_published %>
  </div>
  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.text_field :task_id %>
  </div>
  <div class="field">
    <%= f.label :document_id %><br />
    <%= f.text_field :document_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
